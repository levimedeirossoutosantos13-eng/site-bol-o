/* Importando uma fonte mais robusta */
@import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700&family=Rajdhani:wght@500;700&display=swap');

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    background: radial-gradient(circle, #1a1a1a 0%, #050505 100%);
    color: #e0e0e0;
    font-family: 'Rajdhani', sans-serif;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    overflow: hidden;
}

/* Container principal com efeito de vidro e borda neon */
.box {
    background: rgba(20, 20, 20, 0.9);
    padding: 40px;
    border-radius: 20px;
    width: 380px;
    text-align: center;
    border: 2px solid #00d2ff;
    box-shadow: 0 0 20px rgba(0, 210, 255, 0.2), inset 0 0 10px rgba(0, 210, 255, 0.1);
    position: relative;
}

/* Título com fonte estilo "Corrida" */
h2 {
    font-family: 'Orbitron', sans-serif;
    text-transform: uppercase;
    letter-spacing: 2px;
    color: #00d2ff;
    margin-bottom: 25px;
    text-shadow: 0 0 10px rgba(0, 210, 255, 0.5);
}

/* Inputs simulando peças de precisão */
input {
    width: 100%;
    padding: 12px 15px;
    margin-bottom: 20px;
    background: #111;
    border: 1px solid #333;
    border-left: 4px solid #00d2ff;
    border-radius: 5px;
    color: #fff;
    font-size: 16px;
    transition: 0.3s;
}

input:focus {
    border-color: #fff;
    box-shadow: 0 0 15px rgba(0, 210, 255, 0.4);
    outline: none;
}

/* Botão estilo "Start Engine" */
button {
    width: 100%;
    padding: 15px;
    background: linear-gradient(90deg, #00d2ff, #3a7bd5);
    border: none;
    border-radius: 5px;
    color: #000;
    font-family: 'Orbitron', sans-serif;
    font-weight: bold;
    text-transform: uppercase;
    cursor: pointer;
    transition: 0.4s;
}

button:hover {
    transform: scale(1.02);
    box-shadow: 0 0 25px rgba(0, 210, 255, 0.6);
    color: #fff;
}

/* Links de troca */
.link {
    display: block;
    margin-top: 20px;
    color: #888;
    font-size: 14px;
    cursor: pointer;
    transition: 0.3s;
}

.link:hover {
    color: #00d2ff;
    text-decoration: underline;
}

/* Área Secreta (onde você vai criar o seu local) */
#tela-sucesso {
    border-color: #00ff88;
    box-shadow: 0 0 20px rgba(0, 255, 136, 0.2);
}

#tela-sucesso h2 {
    color: #00ff88;
    text-shadow: 0 0 10px rgba(0, 255, 136, 0.5);
}